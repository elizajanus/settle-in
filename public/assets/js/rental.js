
$(document).ready(function() {
    var postTitle = $("");
    var landlordName = $("");
    // double check the buttons and the data that will be submitted here for reviews

    $(document).on("submit", "#", handleReviewFormSubmit);
    $(document).on("click", "#", handleDeleteButtonPress);

    get
})

$(document).ready(function () {
    $('.slider').slider();
});
