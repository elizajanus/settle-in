<body>
  <div class="jumbotron">
    <h1 style="text-align:center">Meet Your New Roommate Today!</h1>
  </div>

  <div class="container">
    <div class="row">
      <div id="roommateContainer" class="col-md-4">
        
        {{#each roommates}}
        {{> roommates/roommate-block }}

        {{else}}
       <div class="panel panel-default" width="500" height="500">
          <div class="panel-heading">
            <h3 class="panel-title"> No Roommates Available </h3>
          </div>
          <div class="panel-body">
            </div>
       
          {{/each}}

</body>
